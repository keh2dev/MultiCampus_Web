package 조건문;

import javax.swing.JOptionPane;

public class 추가문제 {

	public static void main(String[] args) {
		// 추가문제!
		// 1. 두 수와 사칙연산자를 입력받아 두 수를 사칙연산자에 따라 처리후 출력
		// 2. 두 수를 입력받아 정수로 변환하여 정수변수에  저장, 나누기 연산 수행후 소숫점 2째자리까지 출력!
		double num1 = Double.parseDouble(JOptionPane.showInputDialog("실수1"));
		double num2 = Double.parseDouble(JOptionPane.showInputDialog("실수2"));
		String calcString = JOptionPane.showInputDialog("사칙연산자");
		char calcChar = calcString.charAt(0);
		switch (cal) {
		case value:
			
			break;

		default:
			break;
		}
	}

}
